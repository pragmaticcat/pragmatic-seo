{% import "_includes/forms" as forms %}

{{ forms.textField({
  label: 'Default title'|t('pragmatic-seo'),
  name: 'defaultTitle',
  value: field.defaultTitle,
}) }}

{{ forms.textareaField({
  label: 'Default description'|t('pragmatic-seo'),
  name: 'defaultDescription',
  value: field.defaultDescription,
  rows: 3,
}) }}

{{ forms.textField({
  type: 'number',
  label: 'Default image (Asset ID)'|t('pragmatic-seo'),
  name: 'defaultImageId',
  value: field.defaultImageId,
}) }}

{{ forms.textField({
  label: 'Default image description'|t('pragmatic-seo'),
  name: 'defaultImageDescription',
  value: field.defaultImageDescription,
}) }}
